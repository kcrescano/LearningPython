# import
import math # import math library
from math import * # imports all math module contains.
# difference from import math --> can use function without "math."
from math import pi # import .pi from math only
# use 'as' for alias
import math as m # import math library
# relative imports
from . import math # same directory
from .. import math # go up 1 level directory


#**************************************************************************
pip --version # or -v

# You can use the following command to install:
python get-pip.py  # or python3 get-pip.py, depending on your Python version

# To install a package using pip use the following command:
pip install package_name

# For example, to install the numpy package you would use:
pip install numpy
